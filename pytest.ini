[pytest]
testpaths = tests
pythonpath = src
filterwarnings =
    # Peringatan dari dependency pihak ketiga (starlette TestClient, pytz),
    # bukan dari kode ini. Disenyapkan supaya kegagalan yang sebenarnya tidak
    # tenggelam di antara noise.
    ignore::DeprecationWarning
