# conda env create -f environment.yml && conda activate WIZ.AI
name: WIZ.AI
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pandas=3.0.5
  - pytest=9.1.1
  - pip
  - pip:
      - rapidfuzz==3.14.6
      - fastapi==0.141.1
      - uvicorn==0.53.0
      - pydantic==2.13.5
      - httpx==0.28.1
